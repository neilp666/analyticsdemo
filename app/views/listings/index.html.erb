<div class="jumbotron center">
  <h2>Find a great career in the Analytics Industry Today!</h2>
  <p>This is a great website for anyone looking for analytics jobs in 
    the analytics industry. Don't search an endless list of websites with only a few
    job postings. Out staff is dedicated to finding every open analytics job on the web
  <p>
    <%= link_to "Post a job", new_listing_path , class: "btn btn-primary btn-lg" %>
  <p>
</div>

<table class="table table-striped table-bordered">
    <tr>
      <th class="center">Job</th>
      <th class="center">Description</th>
      <th class="center">Salary</th>
      <th class="center">Location</th>
      <th class="center">Posted</th>
    </tr>

    <% @listings.each do |listing| %>
      <tr>
        <td class="center"><%= listing.job %></td>
        <td class="center"><%= listing.description %></td>
        <td class="center"><%= number_to_currency(listing.salary) %></td>
        <td class="center"><%= listing.location %></td>
        <td class="center"><%= listing.created_at.strftime("%B %-d, %Y") %></td>
        
        
        <td class="center"><%= link_to 'Apply Now', listing, class: "btn btn-primary" %></td>
      </tr>
    <% end %>
</table>

<br>

<% if user_signed_in? %>
  <%= link_to 'Post a New Job', new_listing_path, class: "btn btn-link" %>
<% end %>
